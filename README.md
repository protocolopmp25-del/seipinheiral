
  # Clone Layout into Single Page

  This is a code bundle for Clone Layout into Single Page. The original project is available at https://www.figma.com/design/H3SGSVExTKD9ucDeCd9WT9/Clone-Layout-into-Single-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  